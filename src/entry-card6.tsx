import React from 'react'
import ReactDOM from 'react-dom/client'
import { SpiralInterfaceCard } from '../Shader 6/card'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <SpiralInterfaceCard />
    </React.StrictMode>
)
