import React from 'react'
import ReactDOM from 'react-dom/client'
import RainingLetters from '../Shader 10/shader'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <RainingLetters />
    </React.StrictMode>
)
