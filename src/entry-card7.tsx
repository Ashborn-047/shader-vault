import React from 'react'
import ReactDOM from 'react-dom/client'
import { DottedSurfaceCard } from '../Shader 7/card'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <DottedSurfaceCard />
    </React.StrictMode>
)
