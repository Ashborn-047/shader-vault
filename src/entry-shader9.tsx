import React from 'react'
import ReactDOM from 'react-dom/client'
import SilkDemo from '../Shader 9/shader'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <SilkDemo />
    </React.StrictMode>
)
