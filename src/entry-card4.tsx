import React from 'react'
import ReactDOM from 'react-dom/client'
import AuroraCard from '../Shader 4/card'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <AuroraCard />
    </React.StrictMode>
)
